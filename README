Fonctions :
-> par representation : nombre total de billet vendu, reservées, nombre par tarif

Politique tarifaire :
->voir lesquels on choisit
->trigger
-> attention a l'insert quand tarif deja indiqué => Que normal ou reduit et appliqué une reduction dans le trigger si possible



->fonction permettant d'inserer plus facilement ? (reserver et achete tickets deja fait pour permettre d'en faire plusieurs)
->preparer les requetes que l'on veut faire pendant la soutenance
-> rapport
-> les deux schemas
